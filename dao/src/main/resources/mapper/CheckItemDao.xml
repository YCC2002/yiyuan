<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xncj.dao.CheckItemDao">

    <insert id="addCheckItem" parameterType="org.xncj.pojo.CheckItem">
        insert into t_checkitem(code, name, sex, age, price, type, remark, attention)
        values (#{code}, #{name}, #{sex}, #{age}, #{price}, #{type}, #{remark}, #{attention})
    </insert>
    <update id="updateCheckItem">
        update t_checkitem
        set code=#{code},
            name=#{name},
            sex=#{sex},
            age=#{age},
            price=#{price},
            type=#{type},
            remark=#{remark},
            attention=#{attention}
        where id = #{id}
    </update>
    <delete id="deleteCheckItem">
        delete
        from t_checkitem
        where id = #{id}
    </delete>
    <select id="findPage" resultType="org.xncj.pojo.CheckItem" parameterType="java.lang.String">
        select * from t_checkitem
        <if test="value != null and value.length > 0">
            where code like '%${value}%' or name like '%${value}%'
        </if>
    </select>
    <select id="findAll" resultType="org.xncj.pojo.CheckItem">
        select *
        from t_checkitem
    </select>

</mapper>